# AGENTS

- This repo is `mx8-media`.
- Product and API decisions belong in repo docs, not in this file.
- Use `docs/` for project decisions and reference `../mx8-runtime-reference/` only when needed.
- Core implementation is in Rust.
- The public API is exposed in Python.
- Use idiomatic Rust.
- Do not use `unwrap()`.
- Follow good programming practices for safety, error handling, readability, and maintainability.
